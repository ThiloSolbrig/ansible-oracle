---
bugfixes:
  - GI stack has to be started after opatch removed patches, subsequent opatchauto run fails otherwise"
  - oracle_sw_patches must be addressed by their unique_patchid, if provided
  - Refresh list of applied patches after patch removal
minor_changes:
  - "opatch patches don't need to be downloaded if they're only intended to be removed (opatchauto patches need to be downloaded, though)"
